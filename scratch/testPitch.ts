function detectPitch(buffer: Uint8Array, sampleRate: number): number {
    
    const normalized=new Float32Array(buffer.length)
    // for (let i=0; i<buffer.length;i++) {
    //     normalized[i]
    // }

}